# Copie ce fichier vers pages.env et remplis les valeurs (Supabase → Project Settings → API).
# La clé anon est publique côté client ; les secrets GitHub Actions écrasent ce fichier s’ils sont définis.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# VITE_SUPABASE_EXPECTED_REF=
